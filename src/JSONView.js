import React from "react";


const JSONView = (props) => {
  
  return (
    <section>
      <textarea
        className="text-area"
        value="hola" />
    </section>
  );
}

export default JSONView;
